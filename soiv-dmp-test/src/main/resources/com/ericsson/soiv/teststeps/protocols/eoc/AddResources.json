{
        "resourceCharacteristics": [{
                "name": "deviceID",
                "value": "{{deviceID}}"
        }
        ],
        "rfs": {
                "id": "{{parentRFSId}}"
        },
        "resourceSpecification": "{{PRSName}}"
}
